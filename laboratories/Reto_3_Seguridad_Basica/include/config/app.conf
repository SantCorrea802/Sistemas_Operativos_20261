#!/usr/bin/env bash

# Basic configuration for Reto 3
DEFAULT_PASSWORD="Temporal1!"
PASSWORD_MAX_DAYS=30
PASSWORD_WARN_DAYS=10

# Custom warning shown near password expiration
CUSTOM_WARNING_MESSAGE="ATENCION: tu clave vencera pronto. Actualizala hoy para evitar bloqueo."
